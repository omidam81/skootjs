header#header
  .ng-isolate-scope(navigation='')
    nav.navbar.ng-scope.trans(ng-class='{trans:isTrans(),reverse:isReverse(),gray:isGray()}', ng-controller='NavigationCtrl')
      .nav-primary-wrapper
        .container-fluid
          .navbar-header
            // ngIf: !isCompanyRegistration()
            button.navbar-toggle.ng-scope(type='button', data-toggle='collapse', data-target='#', ng-if='!isCompanyRegistration()')
              #nav-icon
                span
                span
                span
                span
            // end ngIf: !isCompanyRegistration()
            // ngIf: isCompany()
            // ngIf: isCandidate()
            a.navbar-brand(ui-sref='mainCandidate', href='/')
              // ngIf: !isReverse() && !isGray()
              img.ng-scope(ng-if='!isReverse() && !isGray()', ng-src='https://www.skootjobs.com/images/logo.png', src='https://www.skootjobs.com/images/logo.png')
              // end ngIf: !isReverse() && !isGray()
              // ngIf: isReverse() || isGray()
            // ngIf: isLandingComp() || isLandingCand()
          .nav-primary
            ul.nav.navbar-nav.navbar-right.login-dropdown
              // ngIf: isCandidate()
              // ngIf: isCompany()
              // ngIf: isCandidate() && isHomePageCandidate()
              // ngIf: isCandidate()
              // ngIf: isCompany() && isHomeMain()
              // ngIf: isCompany()
              // ngIf: isCompany()
              li(ui-sref-active='active')
                a(ng-click="openState('search')")
                  i.fa.fa-search(aria-hidden='true')
                  |  Job Search
              // ngIf: isCandidate()
              // ngIf: isCompany()
              li.dropdown(ng-hide='isLogedIn()', ui-sref-active='active')
                a.dropdown-toggle.ng-binding(data-toggle='dropdown', role='button', aria-haspopup='true', aria-expanded='true')
                  i.fa.fa-sign-in(aria-hidden='true')
                  |  Login
                  span.caret
                ul.dropdown-menu(aria-labelledby='login')
                  li(ui-sref-active='active')
                    a(href="user/login", translate='') Candidate Login
                  li(ui-sref-active='active')
                    a(href="company/login", translate='') Employer Login
              li.dropdown(ng-hide='isLogedIn()', ui-sref-active='active')
                a.dropdown-toggle.ng-binding(data-toggle='dropdown', role='button', aria-haspopup='true', aria-expanded='true')
                  i.fa.fa-user-plus(aria-hidden='true')
                  |  Create an account
                  span.caret
                ul.dropdown-menu(aria-labelledby='login')
                  li(ui-sref-active='active')
                    a(ui-sref='candidate-signup', translate='', href='/signup/candidate') Candidate Signup
                  li.active(ui-sref-active='active')
                    a(ui-sref='company-signup', translate='', href='/signup/company') Employer Signup
              // ngIf: isCandidate()
              // ngIf: isCompany()
              // ngIf: isCandidate()
              // ngIf: isCandidate()
              // ngIf: businesses.length > 1 && isCompany()
              // ngIf: businesses.length > 1  && theCurrentBusiness
              // ngIf: businesses.length == 1  && isCompany()
              // ngIf: theCurrentBusiness && isCompany()
              // ngIf: theCurrentBusiness
              // ngIf: isCompany()
      // ngIf: !isHomePageCandidate() && !isTrans()
h1.hiddentitle SkootJobs | Find a Job in Malaysia
#toast-container.ng-scope.toast-top-right(ng-class='[config.position, config.animation]', toaster-options="{'time-out': 3000}")
  // ngRepeat: toaster in toasters
#toast-container.ng-scope.toast-center(ng-class='[config.position, config.animation]', toaster-options="{'time-out': 0, 'position-class': 'toast-center', 'toaster-id': 2}")
  // ngRepeat: toaster in toasters
.body-wrap.relative
  scoot-loading.ng-isolate-scope
    #loader.spinner-wrapper(style='display: none;')
      .skoot-spinner-v2(ng-hide='timeOut')
        .spinner
          .cube1
          .cube2
      // ngIf: isCandidate()
      // ngIf: isCompany()
      .text-center.timeout-msg.ng-hide(ng-show='timeOut')
        img.logo-grayscale(src='https://www.skootjobs.com/images/logo.png')
        p
          | We're sorry, things don't appear to be working at the moment. Please try refreshing the page.
  // uiView:
  .ng-scope(ui-view='')
    .page-has-form.signup.skoot-cover.ng-scope(ng-controller='registerPageController')
      .signup-top
        h1.heading.text-center(translate='') Start Recruiting Candidates On SkootJobs Today
        .form-top-text.text-center.text-inverse.hidden-xs
          p.ng-binding Over 8,000+ companies in Malaysia have joined us
        .form-top-text.text-center.text-inverse.hidden-xs
          p(translate='') What Are You Waiting For?
        .clearfix
      .form-container-wrapper
        form.ng-invalid.ng-invalid-required.ng-valid-pattern.ng-valid-minlength.ng-valid-maxlength.ng-valid-email.ng-dirty(name='companyRegister', ng-submit='companySubmit(companyRegister, company)', novalidate='')
          .inputs-container
            input#company-name.form-input.ng-pristine.ng-untouched.ng-empty.ng-invalid.ng-invalid-required.ng-valid-pattern.ng-valid-minlength.ng-valid-maxlength(name='companyname', ng-model='company.companyName', type='text', ng-minlength='2', placeholder='Company name', ng-maxlength='50', ng-required='true', ng-pattern="/^[a-zA-Z0-9- &+@',.]+$/", required='required')
            // ngIf: ((companyRegister.companyname.$touched || companyRegister.$submitted) && companyRegister.companyname.$invalid)
            hr
            input#company-email.form-input.ng-valid-email.ng-not-empty.ng-dirty.ng-valid.ng-valid-required.ng-touched(name='company-email', ng-model='company.email', ng-change="companyRegister['candidate-email'].$setValidity('uniqueness', true)", type='email', placeholder='Email', ng-required='true', required='required')
            ul.error-list.ng-hide(ng-show="((companyRegister['company-email'].$touched || companyRegister.$submitted) && companyRegister['company-email'].$invalid)")
              li.form-error.ng-hide(ng-show="companyRegister['company-email'].$error.required", translate='') Email is required.
              li.form-error.ng-hide(ng-show="companyRegister['company-email'].$error.email", translate='') Should be valid email address.
              li.form-error.ng-hide(ng-show="companyRegister['company-email'].$error.uniqueness", translate='') An account with this email address already exists
            hr
            input#company-password.form-input.ng-pristine.ng-untouched.ng-empty.ng-valid-pattern.ng-invalid.ng-invalid-required(ng-model='company.password', type='password', name='company-password', placeholder='Password', pattern='.{6,16}', ng-required='true', required='required')
            ul.error-list.ng-hide(ng-show="((companyRegister['company-password'].$touched || companyRegister.$submitted) && companyRegister['company-password'].$invalid)")
              li.form-error(ng-show="companyRegister['company-password'].$error.required", translate='') Should not be empty.
              li.form-error.ng-hide(ng-show="companyRegister['company-password'].$error.pattern", translate='') Should be 6 to 16 characters long.
          //
            <div class="login-fb">
            <a href="{{apiDomain}}/auth/fb/login">
            <i class="fa fa-facebook-square" aria-hidden="true"></i><span>Sign Up Using Facebook</span>
            </a>
            </div>
          // ngIf: loading
          // ngIf: !loading
          button.form-btn-submit.ng-scope(ng-if='!loading', type='submit', translate='') Sign Up
          // end ngIf: !loading
      .col-xs-12.form-bottom-options.text-inverse.text-center
        | Already have a SkootJobs account? 
        a(ui-sref='company-login', href='/company/login')
          span Log In
      .col-xs-12.form-bottom-options.text-inverse.text-center
        | By signing up you agree to the 
        a(ui-sref='privacy-policy', href='/privacy-policy') Privacy Policy, 
        a(ui-sref='terms-of-use', href='/terms-of-use') Terms Of Use
      .clearfix


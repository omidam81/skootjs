extends layout

block content
  include _paginate
  h1= title
  h1 jobs
  a(href=paginate.href({ sort: 'name' }))
  ul
   each job in jobs
    li
     h2= job.title
     h1= job.businessId.name
     h2= job.businessId.profileImg
     span= job.typeOfSalary
     span= job.informalAddress
